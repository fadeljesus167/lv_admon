<%= form_with model: @student do |form| %>
    <div>
        <%= form.label :name %>
        <%= form.text_field :name %>
    </div>

    <div>
        <%= form.label :grade %>
        <%= form.select :grade, Student.grades %>
    </div>

    <%= form.submit %>
<% end %>